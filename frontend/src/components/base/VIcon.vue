<script setup lang="ts">
import { computed } from 'vue'
import {
  AlertCircle,
  AlertTriangle,
  Archive,
  ArrowDown,
  ArrowRight,
  ArrowUp,
  ArrowUpDown,
  BookOpen,
  Calendar,
  CalendarClock,
  Check,
  CheckCircle,
  CheckCircle2,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  ChevronsUpDown,
  Circle,
  ClipboardCheck,
  Clock,
  Columns3,
  Copy,
  Download,
  Eye,
  EyeOff,
  FileText,
  FileUp,
  Flag,
  FlaskConical,
  FolderTree,
  GitMerge,
  GraduationCap,
  IdCard,
  Inbox,
  Info,
  KeyRound,
  Layers,
  LayoutDashboard,
  Lightbulb,
  Link,
  ListFilter,
  Loader,
  Lock,
  LockOpen,
  LogOut,
  Mail,
  Microscope,
  MinusCircle,
  Monitor,
  MoreVertical,
  Pencil,
  Play,
  Plus,
  RefreshCw,
  RotateCcw,
  ScrollText,
  Search,
  Send,
  Settings,
  ShieldX,
  Trash2,
  Upload,
  UploadCloud,
  User,
  UserCheck,
  UserPlus,
  UserX,
  WifiOff,
  X,
  XCircle,
} from '@lucide/vue'

/**
 * Explicit name → component registry (rather than `import * as icons`) so
 * Rollup can tree-shake everything from @lucide/vue except what's actually
 * used — the wildcard import was pulling the entire 1000+ icon package into
 * the production bundle, fetched on every page load.
 */
const ICONS: Record<string, unknown> = {
  'alert-circle': AlertCircle,
  'alert-triangle': AlertTriangle,
  archive: Archive,
  'arrow-down': ArrowDown,
  'arrow-right': ArrowRight,
  'arrow-up': ArrowUp,
  'arrow-up-down': ArrowUpDown,
  'book-open': BookOpen,
  calendar: Calendar,
  'calendar-clock': CalendarClock,
  check: Check,
  'check-circle': CheckCircle,
  'check-circle-2': CheckCircle2,
  'chevron-down': ChevronDown,
  'chevron-left': ChevronLeft,
  'chevron-right': ChevronRight,
  'chevron-up': ChevronUp,
  'chevrons-up-down': ChevronsUpDown,
  circle: Circle,
  'clipboard-check': ClipboardCheck,
  clock: Clock,
  'columns-3': Columns3,
  copy: Copy,
  download: Download,
  eye: Eye,
  'eye-off': EyeOff,
  'file-text': FileText,
  'file-up': FileUp,
  flag: Flag,
  'flask-conical': FlaskConical,
  'folder-tree': FolderTree,
  'git-merge': GitMerge,
  'graduation-cap': GraduationCap,
  'id-card': IdCard,
  inbox: Inbox,
  info: Info,
  'key-round': KeyRound,
  layers: Layers,
  'layout-dashboard': LayoutDashboard,
  lightbulb: Lightbulb,
  link: Link,
  'list-filter': ListFilter,
  loader: Loader,
  lock: Lock,
  'lock-open': LockOpen,
  'log-out': LogOut,
  mail: Mail,
  microscope: Microscope,
  'minus-circle': MinusCircle,
  monitor: Monitor,
  'more-vertical': MoreVertical,
  pencil: Pencil,
  play: Play,
  plus: Plus,
  'refresh-cw': RefreshCw,
  'rotate-ccw': RotateCcw,
  'scroll-text': ScrollText,
  search: Search,
  send: Send,
  settings: Settings,
  'shield-x': ShieldX,
  'trash-2': Trash2,
  upload: Upload,
  'upload-cloud': UploadCloud,
  user: User,
  'user-check': UserCheck,
  'user-plus': UserPlus,
  'user-x': UserX,
  'wifi-off': WifiOff,
  x: X,
  'x-circle': XCircle,
}

const props = withDefaults(
  defineProps<{
    name: string
    size?: number
    color?: string
  }>(),
  {
    size: 18,
    color: 'currentColor',
  },
)

const icon = computed(() => {
  const resolved = ICONS[props.name] ?? null
  if (import.meta.env.DEV && !resolved) {
    console.warn(`[VIcon] Unknown icon name: "${props.name}"`)
  }
  return resolved
})
</script>

<template>
  <component :is="icon" v-if="icon" :size="size" :color="color" aria-hidden="true" focusable="false" />
</template>
